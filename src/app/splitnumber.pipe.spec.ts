import { SplitnumberPipe } from './splitnumber.pipe';

describe('SplitnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
